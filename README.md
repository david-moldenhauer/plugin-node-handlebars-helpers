# Pattern Lab Node Plugin - Handlebars Helpers 

This is a collection of Handlebars helpers for Pattern lab.

Make sure the plugin is enabled in your `patternlab-config.json` file:
```
"plugins": {
  "plugin-node-handlebars-helpers": {
    "enabled": true
  }
}
```